# Ensure all Python and shell scripts always use LF line endings
# (prevents CRLF shebang breakage when editing on Windows)
*.py    text eol=lf
*.sh    text eol=lf
*.launch text eol=lf
*.yaml  text eol=lf
*.md    text eol=lf
